{
    "name": "react-movie-relay",
    "version": "1.0.0",
    "description": "An app to fetch movie data from an API through a proxy server",
    "private": "true",
    "source": "./src/index.html",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "parcel build ./src/index.html --public-url ./",
        "start": "parcel"
    },
    "devDependencies": {
        "parcel": "^2.9.1",
        "process": "^0.11.10"
    },
    "dependencies": {
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    }
}
